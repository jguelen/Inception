events {

}

http {
    include /etc/nginx/mime.types;
    server {
        listen 5000;
        server_name _static_;
        root /var/www/static;
        index index.html;
        
        location / {
            try_files $uri $uri/ =404;
        }
    }
}